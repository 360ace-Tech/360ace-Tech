'use client';

// Globe loader replaces the previous CSS orbit + cloud scene.
// The export name is kept so variants/v2/hero.tsx needs no change.
export { GlobeScene as HeroOrbitScene } from './globe-loader';
